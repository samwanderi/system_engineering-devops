#!/usr/bin/env bash
# This script lists the files in the current directory and displays only the part after the first dash

for file in *; do
    # Skip hidden files
    if [[ $file == .* ]]; then
        continue
    fi
    # Extract and print part after the first dash
    echo "${file#*-}"
done
